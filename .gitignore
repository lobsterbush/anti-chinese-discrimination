# Unencrypted source is kept locally only.
# The public repo ships the encrypted docs/index.html.
slides.html
images/
build/
*.pyc
.DS_Store

# Staticrypt state + default output dir
.staticrypt.json
encrypted/

